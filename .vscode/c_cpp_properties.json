{
    "configurations": [
        {
            "name": "Linux",
            "includePath": [
                "${workspaceFolder}/include",   // Add this to point to your include folder
                "${workspaceFolder}/src",       // Optionally, you can add the src folder as well
                "${workspaceFolder}/**",        // This will include all directories in your workspace
                "/usr/include/**"               // System headers
            ],
            "defines": [],
            "compilerPath": "/usr/bin/gcc",
            "cStandard": "gnu17",
            "cppStandard": "gnu++17",
            "intelliSenseMode": "linux-gcc-x64"
        }
    ],
    "version": 4
}
